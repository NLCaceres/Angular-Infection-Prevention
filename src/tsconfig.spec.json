{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/spec",
    "types": ["jest", "node", "@angular/localize"]
  },
  "files": ["test.ts"], // Test.ts is not actually necessary BUT keep it for now
  "include": ["**/*.spec.ts", "**/*.d.ts"]
}
